GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://127.0.0.1:3000/auth/github/callback
# Optional: 32+ char secret for AES-256-GCM token encryption (else .data/token.key)
# TOKEN_ENCRYPTION_KEY=
# Optional: HMAC secret for signed session cookies (else .data/session.key)
# SESSION_SECRET=
